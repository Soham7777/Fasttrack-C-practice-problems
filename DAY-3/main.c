#include <stdio.h>
#include <stdlib.h>
#include "DAY_3.h"
#include<assert.h>
#include<math.h>

#define concat(a,b,c) a##b##c

#define SQUARE(a) (a * a)

int main()
{

    /* assert(convert(1101) == 13);
    assert(convert(11111111) == 255);

    generate_pattern(8);

    assert(power(2,3) == 8);
    assert(power(5,2) == 25);

    assert(find_factorial(2) == 2);
    assert(find_factorial(4) == 24);

    assert(concat(1,2,3) == 123);
    assert(concat(0,1,0) == 010);

    assert(SQUARE(2) == 4);
    assert(SQUARE(4) == 16);*/

    calculator();

    return 0;
}
