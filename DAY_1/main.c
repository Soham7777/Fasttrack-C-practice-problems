#include <stdio.h>
#include <stdlib.h>
#include "DAY1_header.h"
#include <math.h>
#include <assert.h>
int main()
{

     //Uppercase_to_lowercase();

     //area_of_circle();

     //simple_interest();

     //compound_interest();

     //celsius_to_fahrenheit();

     //fahrenheit_to_celsius();

    //EVEN OR ODD
    /*assert(is_even_or_odd(2) == 1);
    assert(is_even_or_odd(3) == 0);*/

    //LEAP YEAR OR NOT

    /*assert(leapyear_or_not(2020) == 1);
    assert(leapyear_or_not(2021) == 0);*/


    return 0;
}
